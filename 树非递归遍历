//前序遍历
void NonRecursoinPreOrder(BinTree t) {
    if(t==NULL) return ;
    stack<BinTree> tmp;
    while(t!=NULL) {
        if(t->right!=NULL) tmp.push(t->right);
        cout<<t->data;
        t = t->left;
    }
    node* p = NULL;
    while(!tmp.empty()) {
        p = tmp.top();
        tmp.pop();
        while(p!=NULL) {
            if(p->right!=NULL) tmp.push(p->right);
            cout<<p->data;
            p  = p->left;
        }
    }
}

